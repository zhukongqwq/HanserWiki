# C# 版数据本地化（自包含数据目录）

C# 版原与 Python 版共享 `Python/` 下的 `data/` 与 `documents.db`；因应用需要对外分发独立运行，决定将数据复制到 C# 自身目录（自包含），此后 C# 版以自身的文档库维护（增量索引）。索引表结构与 Python 版保持一致（格式兼容，数据互不影响）。此前曾考虑"长期共享"或"可配置数据目录"两种替代方案，最终因分发需求直接选择本地化。
